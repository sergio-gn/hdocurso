{
    "version": "0.2.0",
    "configurations": [
      {
        "name": "Open Specific HTML File",
        "type": "chrome",
        "request": "launch",
        "file": "${workspaceFolder}/index.html", // Path to your HTML file
        "webRoot": "${workspaceFolder}"
      }
    ]
  }